plugins {
    id 'java'
    id "io.spring.dependency-management" version("1.0.5.RELEASE")
}

apply plugin: 'idea'

idea {
    module {
        downloadJavadoc = true
        downloadSources = true
    }
}

group 'com.tekion.accounting.fs'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
    maven { url 'https://tkartifactory.tekioncloud.xyz/repository/tek-product-maven-snapshots'
        credentials  {
            username = "${mavenUser}"
            password = "${mavenPassword}"
        }
    }
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    compile project(':fs-entities')
    compile project(':fs-repos')
    compile project(':common')
    implementation('com.tekion.accounting:accounting-client:1.0.42xc-SNAPSHOT')
    compile("com.tekion.sdk:printClient:1.0.102-hotfix3.1-SNAPSHOT")
    implementation('org.apache.poi:poi:5.0.0')
    implementation('org.apache.poi:poi-ooxml:5.0.0')
    implementation('com.tekion.integration.sdk:tinmasterdataclient:0.0.15-SNAPSHOT')
}
